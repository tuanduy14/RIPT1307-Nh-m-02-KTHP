export default defineConfig({
  plugins: ['@umijs/plugin-layout'],
  compilerOptions: {
    "emitDecoratorMetadata": true,
    "experimentalDecorators": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  }
});
